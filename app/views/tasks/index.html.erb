<h1>To Do</h1>
<div class="container">
  <ul>

    <i class="fa-solid fa-plus" data-bs-toggle="modal" data-bs-target="#newModal"></i>
    <%= render 'form', task: Task.new %>

  <% @tasks.each do |task| %>
  <li><%= link_to "#{task.title}", task_path(task) %></li>
  <% end %>
  </ul>
</div>
